<style>
    .contribution {
        transform: scale(0);
        animation: scale 0.4s ease-in forwards;
    }

    @keyframes scale {
        from { transform: scale(0); }
        to { transform: scale(1); }
    }
</style>