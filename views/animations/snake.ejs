<style>
  .contribution {
    transform: scale(0.75);
  }

  .after_food {
    transform: scale(0);
    animation: scale_up 0.4s ease-out forwards;
  }

  @keyframes scale_up {
    from {
      transform: scale(0);
    }

    to {
      transform: scale(1.25);
    }
  }
</style>